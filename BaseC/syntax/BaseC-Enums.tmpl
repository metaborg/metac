
templates

	EnumItem = <<Identifier> = <Constant>> 
	EnumItem = <<Identifier>>
	
	Enum = <<Exported?> enum <Identifier?> { <EnumItem*; separator=","> }>
	Enum = <<Exported?> enum <Identifier?> { <EnumItem*; separator=",">, }>

    