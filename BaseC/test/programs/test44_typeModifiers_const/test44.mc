module test44{
	exported int32 main(int32 argc, string[] argv) {
  		printf("software >> Are you ready for type modifiers? Let's go with CONST!\n");
  		const int32 x = 10;
    	printf("x = %d\n", x);
    	return 0;
	}    
  	
}